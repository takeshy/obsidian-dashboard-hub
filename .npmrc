# CI installs with npm's default peer-dependency resolution. A personal
# ~/.npmrc setting legacy-peer-deps=true would otherwise make local installs
# drop obsidian's peer dependencies (@codemirror/*) from package-lock.json,
# which then breaks `npm ci` in CI. Pin it here so both resolve the same tree.
legacy-peer-deps=false
